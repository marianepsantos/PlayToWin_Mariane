<h1>Conquistas Cadastradas</h1>

<a href="/jogos/{{jogos.id}}/novaconquista">Cadastrar Conquistas</a>

<table border="1">
  <!-- table row -->
  <tr>
    <!-- table head -->
    <th>Titulo</th>
    <th>Descrição</th>
    <th>Jogo</th>
  </tr>
  {{#each Conquistas}}
    <tr>
      <td>{{this.titulo}}</td>
      <td>{{this.descricao}}</td>
    <td>{{this.jogo}}</td>
    </tr>
  {{/each}}
</table>
