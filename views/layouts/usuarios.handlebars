<h1>Usuários Cadastrados</h1>

<table border="1">
    <tr>
        <!-- table head -->
        <th>Id</th>
        <th>Nickname</th>
        <th>Name</th>
    </tr>
    {{#each usuarios}}
    <tr>
        <td>{{this.id}}</td>
        <td>{{this.nickname}}</td>
        <td>{{this.name}}</td>
    </tr>
    {{/each}}
    </table>